function solve(input){
    let vaucherCost=Number(input[0])
    index=1
    let command=input[index]
    let purchaces=0
    while(command!=="End"){
        if(command.length>8)
    }
}

solve (["300",
"Captain Marvel",
"popcorn",
"Pepsi"])
