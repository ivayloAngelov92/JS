function solve(input){
    // let index=0
    // let command1=input[index]
   let arr=[]

    let command=input.shift()

    while(command!=="end"){
            command='gosho'
        arr.push(command)

        command=input.shift()
    }
}

solve (  ['add 1 8', 'contains 1', 'contains 3', 'print'])